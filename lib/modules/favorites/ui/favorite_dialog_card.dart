import 'package:flutter/material.dart';

AlertDialog buildAlertDialog(BuildContext context) {
  return AlertDialog();
}

Card () => Card();


/*
() async {
        final destination = WordPair.random().asPascalCase;
        await FavoritesDatabase.instance.create(
            Favorite(types: "S8", origin:  "Max-Weber-Platz", destinations: "Laimer Platz,Neuperlach Süd"));
        CustomToast.show(context: context, msg: "Inserted $destination successfully");
        ref.refresh(favoriteListProvider);
      },
 */